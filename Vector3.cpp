#include "Vector3.h"

template <typename T>
math::Vector<3, T> forward{ 0, 0, -1 };

template <typename T>
math::Vector<3, T> right{ 1, 0, 0 };

template <typename T>
math::Vector<3, T> up{ 0, 1, 0 };